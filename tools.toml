#:schema ../vision-tools/src/vision_tools/schemas/tools.schema.json

schema_version = 1

[registry]
name = "sam3-mlx"
repository = "sam3_mlx"
description = "Apple Silicon SAM3 runtime; agent-facing image workflows are provided by FIESTA."

[tools]

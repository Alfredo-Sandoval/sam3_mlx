# flake8: noqa
"""TETA eval toolkit compatibility package."""

from sam3_mlx.eval.teta_eval_toolkit import (
    config as config,
    datasets as datasets,
    metrics as metrics,
    utils as utils,
)
from sam3_mlx.eval.teta_eval_toolkit.eval import Evaluator as Evaluator
